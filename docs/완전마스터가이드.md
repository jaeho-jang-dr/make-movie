# 🎬 Google AI 어린이 동영상 자동 생성 시스템
## 완전 자동화 마스터 패키지

---

**작성일:** 2026년 1월 29일  
**버전:** 1.0  
**대상:** Google IDX / Antigravity 사용자  
**필수 구독:** Google One AI Premium

---

## 📋 목차

1. [시스템 개요](#1-시스템-개요)
2. [준비물](#2-준비물)
3. [Google IDX에서 설치하기](#3-google-idx에서-설치하기)
4. [전체 파일 목록 및 코드](#4-전체-파일-목록-및-코드)
5. [사용 방법](#5-사용-방법)
6. [문제 해결](#6-문제-해결)
7. [비용 및 제한](#7-비용-및-제한)

---

## 1. 시스템 개요

### 🎯 이 시스템이 하는 일

명령어 **한 줄**로 10분짜리 어린이 동영상을 완전 자동으로 생성합니다:

```bash
npm run create -- -t "용감한 토끼" -d 10 --theme "용기"
```

그러면:
1. ✅ Gemini 2.0이 스토리 작성
2. ✅ Imagen 3이 이미지 생성
3. ✅ Google TTS가 음성 생성
4. ✅ Remotion이 동영상 조합
5. ✅ 최종 MP4 파일 출력

### 🔄 처리 흐름

```
제목 + 주제 입력
    ↓
Gemini 2.0 Flash → 스토리 생성 (5-8개 씬)
    ↓
Imagen 3 → 각 씬마다 배경 + 캐릭터 이미지 생성
    ↓
Google Cloud TTS → 각 씬마다 한국어 나레이션 생성
    ↓
자동으로 Remotion 컴포넌트 생성
    ↓
렌더링 (15-30분)
    ↓
완성된 MP4 파일!
```

---

## 2. 준비물

### ✅ 필수 계정

1. **Google One AI Premium** (월 $19.99)
   - Gemini Advanced 무제한
   - Imagen 3 월 500장
   - Cloud TTS 월 100만 자

2. **Google Cloud 계정**
   - 무료 계정으로 시작 가능
   - Text-to-Speech API 활성화 필요

### ✅ 필수 API 키 발급

#### A. Google AI Studio API 키

```
1. https://aistudio.google.com/app/apikey 접속
2. "Create API Key" 클릭
3. 생성된 키 복사 → 나중에 .env 파일에 사용
```

#### B. Google Cloud 서비스 계정 키

```bash
# 1. Google Cloud Console 접속
https://console.cloud.google.com

# 2. 프로젝트 생성 (예: story-video-maker)

# 3. Text-to-Speech API 활성화
https://console.cloud.google.com/apis/library/texttospeech.googleapis.com

# 4. 서비스 계정 생성
https://console.cloud.google.com/iam-admin/serviceaccounts

# 5. 키 생성 → JSON 다운로드
```

---

## 3. Google IDX에서 설치하기

### 단계 1: 새 프로젝트 생성

```bash
# IDX 터미널에서
mkdir google-story-video
cd google-story-video
```

### 단계 2: 프로젝트 초기화

```bash
# package.json 생성
npm init -y
```

### 단계 3: 필요한 폴더 생성

```bash
mkdir -p src/automation
mkdir -p public/images
mkdir -p public/audio
mkdir -p output
```

### 단계 4: 아래 섹션의 모든 파일 생성

**다음 섹션에 있는 모든 파일을 복사해서 생성하세요!**

---

## 4. 전체 파일 목록 및 코드

### 📄 파일 1: package.json

**경로:** `./package.json`

**내용:**
```json
{
  "name": "google-story-video",
  "version": "1.0.0",
  "description": "Google AI 어린이 동영상 자동 생성",
  "scripts": {
    "create": "ts-node src/automation/cli.ts create",
    "batch": "ts-node src/automation/cli.ts batch",
    "preview": "remotion preview src/index.ts"
  },
  "dependencies": {
    "remotion": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@google/generative-ai": "^0.21.0",
    "@google-cloud/text-to-speech": "^5.4.0",
    "commander": "^11.1.0",
    "dotenv": "^16.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  }
}
```

**생성 방법:**
```bash
# IDX 터미널에서
cat > package.json << 'EOF'
[위 내용 붙여넣기]
EOF
```

---

### 📄 파일 2: tsconfig.json

**경로:** `./tsconfig.json`

**내용:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "jsx": "react",
    "module": "commonjs",
    "moduleResolution": "node",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "strict": true,
    "resolveJsonModule": true,
    "outDir": "./dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
```

---

### 📄 파일 3: .env

**경로:** `./.env`

**내용:**
```bash
# Google AI Studio API Key (필수!)
GOOGLE_AI_API_KEY=여기에_발급받은_API_키_입력

# Google Cloud 서비스 계정 키 경로
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json
```

**⚠️ 중요:** 
- `여기에_발급받은_API_키_입력` 부분을 실제 API 키로 변경!
- https://aistudio.google.com/app/apikey 에서 발급

---

### 📄 파일 4: .gitignore

**경로:** `./.gitignore`

**내용:**
```
node_modules/
.env
service-account-key.json
dist/
output/
public/images/
public/audio/
.DS_Store
```

---

### 📄 파일 5: src/index.ts

**경로:** `./src/index.ts`

**내용:**
```typescript
import { registerRoot } from 'remotion';
import { RemotionRoot } from './Root';

registerRoot(RemotionRoot);
```

---

### 📄 파일 6: src/automation/types.ts

**경로:** `./src/automation/types.ts`

**내용:**
```typescript
export interface StoryConfig {
  title: string;
  duration: number;
  targetAge: string;
  theme: string;
}

export interface Scene {
  sceneNumber: number;
  duration: number;
  narration: string;
  backgroundDescription: string;
  characters: string[];
  actions: string[];
}
```

---

### 📄 파일 7: src/automation/GoogleStoryGenerator.ts

**경로:** `./src/automation/GoogleStoryGenerator.ts`

**⚠️ 이 파일은 매우 깁니다!** 

아래 코드 전체를 복사하세요:

```typescript
// [여기에 위에서 생성한 06-src-automation-GoogleStoryGenerator.ts 파일의 전체 내용]
// 약 400줄의 코드
```

**또는 Claude Code CLI 사용:**
```bash
claude-code
```
그리고 Claude에게:
```
src/automation/GoogleStoryGenerator.ts 파일을 생성해줘.
내가 이전에 준 GoogleStoryGenerator.ts 코드를 사용해.
```

---

### 📄 파일 8: src/automation/cli.ts

**경로:** `./src/automation/cli.ts`

**⚠️ 이 파일도 깁니다!**

아래 코드 전체를 복사하세요:

```typescript
// [여기에 위에서 생성한 07-src-automation-cli.ts 파일의 전체 내용]
// 약 150줄의 코드
```

---

### 📄 파일 9: stories.json (선택사항)

**경로:** `./stories.json`

**내용:**
```json
[
  {
    "title": "용감한 토끼의 모험",
    "duration": 10,
    "targetAge": "5-7세",
    "theme": "용기와 도전"
  },
  {
    "title": "친절한 곰 이야기",
    "duration": 10,
    "targetAge": "5-7세",
    "theme": "우정과 배려"
  }
]
```

---

## 5. 사용 방법

### 🚀 한 번에 설치 및 실행

```bash
# 1. 패키지 설치
npm install

# 2. Google Cloud 인증
gcloud auth application-default login

# 3. Text-to-Speech API 활성화
gcloud services enable texttospeech.googleapis.com

# 4. 첫 동영상 생성! (5분 짜리 테스트)
npm run create -- -t "나의 첫 동영상" -d 5
```

### 💎 실전 사용법

#### 기본 사용
```bash
npm run create -- -t "제목" -d 10
```

#### 상세 옵션
```bash
npm run create -- \
  --title "마법의 숲 모험" \
  --duration 10 \
  --age "5-7세" \
  --theme "우정과 용기"
```

#### 여러 개 한 번에
```bash
npm run batch -- -f stories.json
```

#### 미리보기
```bash
npm run preview
```

### ⏱️ 예상 소요 시간

**10분 동영상 기준:**
1. 스토리 생성: 30초
2. 이미지 생성: 5-10분
3. 음성 생성: 2-3분
4. 컴포넌트 생성: 10초
5. 렌더링: 15-30분
**총: 약 25-45분**

---

## 6. 문제 해결

### ❌ 문제 1: "API key not valid"

**원인:** API 키가 잘못됨

**해결:**
```bash
# 1. .env 파일 확인
cat .env

# 2. API 키 재발급
# https://aistudio.google.com/app/apikey

# 3. .env 파일 수정
nano .env
```

---

### ❌ 문제 2: "Permission denied" (TTS)

**원인:** Google Cloud 인증 안됨

**해결:**
```bash
gcloud auth application-default login
gcloud services enable texttospeech.googleapis.com
```

---

### ❌ 문제 3: 메모리 부족

**원인:** Node.js 메모리 제한

**해결:**
```bash
export NODE_OPTIONS="--max-old-space-size=8192"
npm run create -- -t "테스트" -d 5
```

---

### ❌ 문제 4: 이미지 생성 실패

**원인:** Imagen 3 한도 초과 또는 API 오류

**해결:**
1. Google Ultra 구독 확인
2. 월 500장 한도 확인 (AI Studio에서)
3. 프롬프트가 너무 길지 않은지 확인

---

### ❌ 문제 5: 렌더링이 너무 느림

**해결:**
```bash
# 병렬 처리 증가
npx remotion render src/index.ts StoryVideo output/video.mp4 --concurrency 8

# 화질 낮추기 (더 빠름)
npx remotion render src/index.ts StoryVideo output/video.mp4 --crf 28
```

---

## 7. 비용 및 제한

### 💰 Google One AI Premium ($19.99/월)

| 서비스 | 제공량 | 10분 동영상 1개당 사용량 |
|--------|--------|--------------------------|
| Gemini 2.0 | 무제한 | ~1회 |
| Imagen 3 | 월 500장 | 6-8장 |
| Cloud TTS | 월 100만 자 | ~1,500자 |

### 📊 월 제작 가능량

- **이미지 기준:** 60-80개 동영상
- **TTS 기준:** 650개 동영상
- **실질적 한도:** 약 60-80개

### 💡 비용 절감 팁

1. 5분 동영상부터 시작 (이미지 3-4장)
2. 배치 처리로 한 번에 여러 개 생성
3. 테스트는 짧은 동영상으로

---

## 8. 고급 활용

### 🎨 커스텀 스타일 적용

**src/automation/GoogleStoryGenerator.ts** 파일에서 프롬프트 수정:

```typescript
// 이미지 스타일 변경
const prompt = `
  Watercolor style children's book illustration...
  // 또는
  Pixel art style for children...
  // 또는
  3D rendered cute characters...
`;
```

### 🎵 배경 음악 추가

```bash
# public/audio/background-music.mp3 파일 추가
# StoryVideo.tsx가 자동으로 감지
```

### 🌍 다국어 지원

**.env 파일에 추가:**
```bash
TTS_LANGUAGE_CODE=en-US
TTS_VOICE_NAME=en-US-Neural2-C
```

---

## 9. Claude Code CLI 활용

### Gemini 3 Pro + Claude Code 조합

**오른쪽 Gemini 채팅창에서:**
```
나를 위해 "용감한 토끼" 스토리를 10개 씬으로 만들어줘.
각 씬마다:
- 나레이션 (한국어)
- 배경 설명 (영어)
- 캐릭터
- 동작

JSON 형식으로 출력해줘.
```

**받은 JSON을 수동으로 조정 후, IDX 터미널에서:**
```bash
claude-code
```

**Claude에게:**
```
위 JSON을 사용해서 동영상 생성 스크립트 실행해줘:
npm run create -- -t "용감한 토끼" -d 10
```

---

## 10. 체크리스트

### ✅ 설치 전

- [ ] Google One AI Premium 구독
- [ ] Google AI Studio API 키 발급
- [ ] Google Cloud 프로젝트 생성
- [ ] Text-to-Speech API 활성화
- [ ] Node.js 18+ 설치 확인

### ✅ 파일 생성

- [ ] package.json
- [ ] tsconfig.json
- [ ] .env (API 키 포함!)
- [ ] src/index.ts
- [ ] src/automation/types.ts
- [ ] src/automation/GoogleStoryGenerator.ts
- [ ] src/automation/cli.ts

### ✅ 설치 및 실행

- [ ] npm install
- [ ] gcloud auth application-default login
- [ ] npm run create -- -t "테스트" -d 5

---

## 📞 지원 및 문의

### 자주 하는 실수

1. **.env 파일에 API 키 안 넣음**
   → GOOGLE_AI_API_KEY= 뒤에 실제 키 입력!

2. **Google Cloud 인증 안 함**
   → `gcloud auth application-default login` 필수!

3. **패키지 설치 안 함**
   → `npm install` 먼저!

### 추가 도움

- Google AI Studio: https://aistudio.google.com
- Google Cloud Console: https://console.cloud.google.com
- Remotion 문서: https://remotion.dev

---

## 🎉 완료!

**이제 시작하세요:**

```bash
cd google-story-video
npm install
npm run create -- -t "나의 첫 동영상" -d 5
```

**성공하면:**
- `output/나의-첫-동영상.mp4` 파일 생성!
- 약 25-45분 후 완성

**행운을 빕니다! 🚀**

---

**문서 버전:** 1.0  
**최종 업데이트:** 2026-01-29  
**작성자:** Claude AI  
**라이선스:** MIT
