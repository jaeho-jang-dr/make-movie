# 🎬 Google AI 어린이 동영상 자동 생성 시스템
## 완전 통합 패키지 - Google Drive용

---

이 문서는 **모든 필요한 파일과 코드**를 포함하고 있습니다.
각 섹션의 파일을 복사하여 프로젝트에 생성하세요.

---

## 📂 프로젝트 구조

```
google-story-video/
├── package.json
├── tsconfig.json
├── .env.example
├── .env (직접 생성)
├── .gitignore
├── stories.json
├── src/
│   ├── index.ts
│   ├── Root.tsx (자동 생성됨)
│   ├── StoryVideo.tsx (자동 생성됨)
│   └── automation/
│       ├── types.ts
│       ├── GoogleStoryGenerator.ts
│       └── cli.ts
├── public/
│   ├── images/ (자동 생성됨)
│   └── audio/ (자동 생성됨)
└── output/ (자동 생성됨)
```

---

## 🚀 설치 단계

### 1단계: 프로젝트 폴더 생성

```bash
mkdir google-story-video
cd google-story-video
```

### 2단계: 아래 파일들을 각각 생성

---

## 📄 파일 1: package.json

**경로: `./package.json`**

```json
{
  "name": "google-story-video",
  "version": "1.0.0",
  "description": "Google AI를 활용한 어린이 동영상 자동 생성 시스템",
  "main": "src/index.ts",
  "scripts": {
    "create": "ts-node src/automation/cli.ts create",
    "batch": "ts-node src/automation/cli.ts batch",
    "preview": "remotion preview src/index.ts",
    "render": "remotion render src/index.ts StoryVideo output/video.mp4"
  },
  "keywords": ["video", "automation", "ai", "google", "gemini", "remotion"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "remotion": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@google/generative-ai": "^0.21.0",
    "@google-cloud/text-to-speech": "^5.4.0",
    "axios": "^1.6.0",
    "commander": "^11.1.0",
    "dotenv": "^16.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/node": "^20.10.0",
    "@remotion/cli": "^4.0.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0"
  }
}
```

---

## 📄 파일 2: tsconfig.json

**경로: `./tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "jsx": "react",
    "module": "commonjs",
    "moduleResolution": "node",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "strict": true,
    "resolveJsonModule": true,
    "outDir": "./dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "output"]
}
```

---

## 📄 파일 3: .env.example

**경로: `./.env.example`**

```bash
# Google AI Studio API Key
GOOGLE_AI_API_KEY=your-google-ai-api-key-here

# Google Cloud 서비스 계정 키 경로
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json

# 선택사항
TTS_LANGUAGE_CODE=ko-KR
TTS_VOICE_NAME=ko-KR-Neural2-A
```

**실제 .env 파일을 만들 때:**
1. 이 파일을 `.env`로 복사
2. `GOOGLE_AI_API_KEY`에 실제 키 입력
3. https://aistudio.google.com/app/apikey 에서 발급

---

## 📄 파일 4: .gitignore

**경로: `./.gitignore`**

```
node_modules/
.env
service-account-key.json
dist/
output/
public/images/
public/audio/
.DS_Store
*.log
```

---

## 📄 파일 5: stories.json

**경로: `./stories.json`**

```json
[
  {
    "title": "용감한 토끼의 모험",
    "duration": 10,
    "targetAge": "5-7세",
    "theme": "용기와 도전"
  },
  {
    "title": "친절한 곰 이야기",
    "duration": 10,
    "targetAge": "5-7세",
    "theme": "우정과 배려"
  }
]
```

---

## 📄 파일 6: src/index.ts

**경로: `./src/index.ts`**

```typescript
import { registerRoot } from 'remotion';
import { RemotionRoot } from './Root';

registerRoot(RemotionRoot);
```

---

## 📄 파일 7: src/automation/types.ts

**경로: `./src/automation/types.ts`**

```typescript
export interface StoryConfig {
  title: string;
  duration: number;
  targetAge: string;
  theme: string;
}

export interface Scene {
  sceneNumber: number;
  duration: number;
  narration: string;
  backgroundDescription: string;
  characters: string[];
  actions: string[];
}
```

---

## 📄 파일 8: src/automation/GoogleStoryGenerator.ts

**이 파일은 매우 깁니다. 위에서 생성한 `06-src-automation-GoogleStoryGenerator.ts` 파일의 내용을 그대로 복사하세요.**

**경로: `./src/automation/GoogleStoryGenerator.ts`**

---

## 📄 파일 9: src/automation/cli.ts

**이 파일도 깁니다. 위에서 생성한 `07-src-automation-cli.ts` 파일의 내용을 그대로 복사하세요.**

**경로: `./src/automation/cli.ts`**

---

## 🔧 설치 및 실행

### 필수 폴더 생성

```bash
mkdir -p src/automation
mkdir -p public/images
mkdir -p public/audio
mkdir -p output
```

### 패키지 설치

```bash
npm install
```

### Google Cloud 설정

```bash
# 1. Google Cloud SDK 설치 (아직 안했다면)
# Mac: brew install google-cloud-sdk
# Ubuntu: sudo apt-get install google-cloud-sdk

# 2. 인증
gcloud auth application-default login

# 3. Text-to-Speech API 활성화
gcloud services enable texttospeech.googleapis.com
```

### 첫 동영상 생성!

```bash
# 5분 짜리 테스트
npm run create -- -t "나의 첫 동영상" -d 5

# 10분 짜리
npm run create -- -t "용감한 토끼" -d 10 --theme "용기"
```

---

## 💰 비용 정보

### Google One AI Premium ($19.99/월)
- Gemini 2.0: 무제한
- Imagen 3: 월 500장
- TTS: 월 100만 자

### 10분 동영상 1개당
- **총 비용: $0** (Ultra 구독 범위 내)
- 월 60-80개 동영상 제작 가능

---

## 🐛 문제 해결

### API 키 오류
```bash
# .env 파일 확인
cat .env

# API 키 재발급
# https://aistudio.google.com/app/apikey
```

### TTS 권한 오류
```bash
gcloud auth application-default login
```

### 메모리 부족
```bash
export NODE_OPTIONS="--max-old-space-size=8192"
```

---

## 📞 지원

문제가 있으시면:
1. .env 파일이 올바른지 확인
2. Google Cloud 인증 확인
3. API 키 한도 확인

---

## ✅ 체크리스트

설치 전:
- [ ] Node.js 18+ 설치 확인
- [ ] Google AI Studio API 키 발급
- [ ] Google Cloud 프로젝트 생성
- [ ] Text-to-Speech API 활성화

파일 생성:
- [ ] package.json
- [ ] tsconfig.json
- [ ] .env (API 키 포함)
- [ ] src/index.ts
- [ ] src/automation/types.ts
- [ ] src/automation/GoogleStoryGenerator.ts
- [ ] src/automation/cli.ts

실행:
- [ ] npm install
- [ ] gcloud auth application-default login
- [ ] npm run create -- -t "테스트" -d 5

---

**준비 완료! 첫 동영상을 만들어보세요! 🎉**

```bash
npm run create -- -t "마법의 숲 모험" -d 10
```
